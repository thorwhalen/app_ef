# Base URL of the app_ef backend (qh-over-EfService). The backend's documented
# default port is 8000; override here if you run it elsewhere. In production
# tw_platform's deploy injects VITE_API_BASE=/api/app_ef automatically.
VITE_API_BASE=http://localhost:8000
